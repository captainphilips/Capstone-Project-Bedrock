# Copy to terraform.tfvars and adjust if needed.
# Required for reproducibility when customizing deployment.

# aws_region  = "us-east-1"
# environment = "dev"

# EKS Kubernetes version - use 1.29, 1.31, 1.32, 1.33, or 1.34 (1.27 is unsupported)
# cluster_version = "1.29"

# Set to true if bedrock-dev-view IAM user already exists (from partial apply)
# use_existing_bedrock_dev_view_user = true
