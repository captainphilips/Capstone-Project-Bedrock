# Policies

IAM and Kubernetes policy definitions for reference and manual use.

| File | Description |
|------|-------------|
| `lb-controller-policy.json` | AWS Load Balancer Controller IAM policy (reference) |

**Note**: The ALB controller IAM policy is defined inline in `infra/modules/alb_controller/main.tf`. These JSON files serve as documentation and for manual policy creation if needed.
